import { PersonRecord } from './types';

// Iniciar con un arreglo vacío para evitar confusión en los usuarios.
// La aplicación intentará sincronizar automáticamente al inicio.
export const INITIAL_DATA: PersonRecord[] = [];